name: CD - Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v3

      - name: Simular deploy
        run: echo "Deploy da API DevOps realizado com sucesso!"